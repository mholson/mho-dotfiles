# -*- mode: snippet -*-
# name:diu insert json definition
# key: jtd
# group: diu
# --

#import "/typst/lib.typ": *
#let data = json("${1:`(file-name-sans-extension (buffer-name))`.json}")

#insert_(data, includeDefinition: true,(data) => [])