# -*- mode: snippet -*-
# name:diu insert json link
# key: jtl
# group: diu
# --

#import "/typst/lib.typ": *
#let data = json("${1:`(file-name-sans-extension (buffer-name))`.json}")

#insert_(data, includeLink: true,(data) => [])